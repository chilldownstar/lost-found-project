# Real DB credentials — never commit, use config.example.php as a template
config.php

# One-time admin password reset script — should not exist in production/public repo
change_pass.php

# User-uploaded images (keep the folder, ignore its contents)
uploads/*
!uploads/.gitkeep

# OS / editor junk
.DS_Store
.vscode/
.idea/
*.log
